-- 1時間前より古いデータを一括削除（JST）
CREATE OR ALTER PROCEDURE dbo.usp_Purge_Telemetry_Before1Hour_JST
AS
BEGIN
    SET NOCOUNT ON;
    SET DEADLOCK_PRIORITY LOW;

    DELETE FROM dbo.Telemetry
    WHERE TS < DATEADD(hour, -1, SYSDATETIME());  -- いまから1時間前より古い行
END
GO
