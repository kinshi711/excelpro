SELECT CONVERT(varchar(19), RunAt, 120)  AS RunAt,
       CONVERT(varchar(19), Cutoff, 120) AS Cutoff,
       COUNT(*) AS DeletedCount
FROM PlantDB.dbo.PurgeLogDetail
GROUP BY RunAt, Cutoff
ORDER BY RunAt DESC;

